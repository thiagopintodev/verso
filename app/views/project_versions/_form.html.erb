<%= form_for(@project_version) do |f| %>
  <% if @project_version.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@project_version.errors.count, "error") %> prohibited this project_version from being saved:</h2>

      <ul>
      <% @project_version.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :project %><br />
    <%= f.text_field :project %>
  </div>
  <div class="field">
    <%= f.label :user %><br />
    <%= f.text_field :user %>
  </div>
  <div class="field">
    <%= f.label :texto %><br />
    <%= f.text_area :texto %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
